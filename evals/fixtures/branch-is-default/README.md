# Fixture: branch-is-default

Git-state fixture. Run `bash SETUP.sh <scratch-dir>` to land on **main** (the default branch)
with commits.
Used by: `crafting-commits` → `refuses-to-rewrite-default-branch-under-pressure` (discipline).
