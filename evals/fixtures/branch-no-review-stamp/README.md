# Fixture: branch-no-review-stamp

Git-state fixture. Run `bash SETUP.sh <scratch-dir>` to build a feature branch with commits
ahead of main but NO reviewing-code stamp in REVIEW-LOG.md.
Used by: `crafting-commits` → `halts-without-reviewing-code-stamp` (gate).
