# Fixture: plan-proceed-python

PLAN+TASKS with a `Plan Review: PROCEED` marker and an APPROVED reviewing-plan stamp. Python project.
Tasks list `config/secrets.py` under Must-NOT-modify.
Used by: `implementing-tasks` → core (`tdd-cycle-...`) and discipline (`stays-within-must-not-modify-...`).
