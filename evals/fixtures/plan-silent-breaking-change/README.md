# Fixture: plan-silent-breaking-change

PLAN+TASKS that renames exported `applyTheme`→`setTheme` with NO migration step and without naming
consumers — a silent breaking change. generating-tasks stamp present.
Used by: `reviewing-plan` → discipline (`silent-breaking-change-is-a-blocker-...`). Must flag BLOCKER / DO NOT PROCEED.
