# Fixture: plan-with-planning-stamp

PLAN+ticket with an APPROVED **planning-from-spec** stamp; no tasks yet.
Used by: `generating-tasks` → core (`appends-...`) and discipline (`asks-rather-than-guessing-...`).
