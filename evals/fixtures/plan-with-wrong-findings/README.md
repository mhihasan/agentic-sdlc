# Fixture: plan-with-wrong-findings

reviewing-plan findings that are WRONG: T3 (OS-default) IS ticket-traceable (AC exists), and
`usePersistentPref` DOES exist (see plan Key findings). Both should be pushed back.
Used by: `receiving-plan-review` → discipline (`pushes-back-on-wrong-findings-...`).
