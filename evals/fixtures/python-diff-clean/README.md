# Fixture: python-diff-clean

A small, correct pure-Python staged diff (`DIFF.md`) with all implementing-tasks stamps present
(`REVIEW-LOG.md`, one task).
Used by: `reviewing-code` → core (`proportional-review-with-severity-verdict`). Expect Python
stack detected, only checks-universal.md loaded, structured PASS-ish verdict.
